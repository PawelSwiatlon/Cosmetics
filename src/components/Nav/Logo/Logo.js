import React from 'react';
import styles from './Logo.module.scss';

const Logo = () => {
    return(
        <p className={styles.wrapper}>
            Cosmetics
        </p>
    )
}

export default Logo;