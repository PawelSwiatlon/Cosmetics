import React from 'react';
import styles from './Logo.module.scss';

const Logo = () => {
    return(
        <div className={styles.wrapper}>
            <p>Cosmetic</p>
        </div>
    )
}

export default Logo;